<Window x:Class="GlassShot.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GlassShot Preferences"
        Icon="icon.ico"
        Width="780" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#181818"
        Foreground="White"
        FontFamily="Segoe UI, Inter, Arial, sans-serif">

    <Window.Resources>
        <!-- Style for Sidebar Navigation Buttons -->
        <Style x:Key="SidebarButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Background="{TemplateBinding Background}">
                            <!-- Accent indicator line on left -->
                            <Border x:Name="AccentLine" Width="3" HorizontalAlignment="Left" Background="#3A82F7" Visibility="Hidden"/>
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#252525"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="AccentLine" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#202020"/>
                                <Setter Property="Foreground" Value="#EEEEEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for Grouped Settings Cards -->
        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="BorderBrush" Value="#2E2E2E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Padding" Value="18,16"/>
        </Style>

        <!-- Style for TextBoxes / Hotkeys -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#141414"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3A82F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Item Template -->
        <Style x:Key="ModernComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2,1">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#303030"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#3A82F7"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ToggleButton Template for ComboBox -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="28"/>
                    </Grid.ColumnDefinitions>
                    <!-- Chevron arrow -->
                    <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4.5 4.5 L 9 0" Stroke="#AAAAAA" StrokeThickness="1.5" Fill="Transparent"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#252525"/>
                    <Setter TargetName="Border" Property="BorderBrush" Value="#505050"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Border" Property="BorderBrush" Value="#3A82F7"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Full Dark Modern ComboBox Template -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0,0,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ModernComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Template="{StaticResource ComboBoxToggleButton}" 
                                          Background="{TemplateBinding Background}" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          Focusable="false" 
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite" 
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="Center" 
                                              HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup Name="Popup" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown" 
                                      SnapsToDevicePixels="True" 
                                      MinWidth="{TemplateBinding ActualWidth}" 
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Background="#1B1B1B" 
                                            BorderBrush="#383838" 
                                            BorderThickness="1" 
                                            CornerRadius="6" 
                                            Margin="0,4,0,4" 
                                            Padding="4">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="12" ShadowDepth="4" Direction="270" Opacity="0.45" Color="Black"/>
                                        </Border.Effect>
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDown" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter Property="BorderBrush" Value="#3A82F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar Navigation -->
        <Border Grid.Column="0" Background="#111111" BorderBrush="#242424" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20,0,0">
                <!-- App Header -->
                <StackPanel Orientation="Horizontal" Margin="20,0,0,25">
                    <Border Width="30" Height="30" CornerRadius="8" ClipToBounds="True">
                        <Image Source="Assets/icon_256.png" Width="30" Height="30"/>
                    </Border>
                    <TextBlock Text="GlassShot" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>

                <RadioButton x:Name="TabGeneral" Content="⚙️   General" GroupName="Sidebar" Style="{StaticResource SidebarButton}" IsChecked="True" Checked="Tab_Checked"/>
                <RadioButton x:Name="TabShortcuts" Content="⌨️   Shortcuts" GroupName="Sidebar" Style="{StaticResource SidebarButton}" Checked="Tab_Checked"/>
                <RadioButton x:Name="TabQuickAccess" Content="🖼️   Quick Access" GroupName="Sidebar" Style="{StaticResource SidebarButton}" Checked="Tab_Checked"/>
                <RadioButton x:Name="TabRecording" Content="🎥   Recording" GroupName="Sidebar" Style="{StaticResource SidebarButton}" Checked="Tab_Checked"/>
                <RadioButton x:Name="TabAbout" Content="ℹ️   About" GroupName="Sidebar" Style="{StaticResource SidebarButton}" Checked="Tab_Checked"/>
            </StackPanel>
        </Border>

        <!-- Right Content Area -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="28,24">
                <!-- GENERAL PANEL -->
                <StackPanel x:Name="PanelGeneral" Visibility="Visible">
                    <TextBlock Text="General Settings" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,18"/>
                    
                    <Border Style="{StaticResource SettingsCard}">
                        <StackPanel>
                            <TextBlock Text="System Behavior" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,14"/>
                            <CheckBox x:Name="ChkStartup" Content="Launch GlassShot automatically at Windows startup" Foreground="#E0E0E0" FontSize="14" Margin="0,0,0,12" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                            <CheckBox x:Name="ChkSound" Content="Play sound effect after taking a screenshot or recording" Foreground="#E0E0E0" FontSize="14" Margin="0,0,0,12" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                            <CheckBox x:Name="ChkHideIcons" Content="Automatically hide desktop icons during screen capture" Foreground="#E0E0E0" FontSize="14" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SettingsCard}">
                        <StackPanel>
                            <TextBlock Text="Save Location" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,6"/>
                            <TextBlock Text="Default folder where standalone recordings and exported images are saved." FontSize="12" Foreground="#999999" Margin="0,0,0,12" TextWrapping="Wrap"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtSaveDir" Grid.Column="0" Style="{StaticResource ModernTextBox}" IsReadOnly="True" Margin="0,0,10,0"/>
                                <Button Grid.Column="1" Content="Browse..." Width="85" Height="34" Background="#3A82F7" Foreground="White" BorderThickness="0" Cursor="Hand" Click="Browse_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- SHORTCUTS PANEL -->
                <StackPanel x:Name="PanelShortcuts" Visibility="Collapsed">
                    <TextBlock Text="Keyboard Shortcuts" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,6"/>
                    <TextBlock Text="Click in any shortcut box and press a combination of keys (e.g. Ctrl + Shift + C)." FontSize="13" Foreground="#999999" Margin="0,0,0,18" TextWrapping="Wrap"/>

                    <Border Style="{StaticResource SettingsCard}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="42"/>
                                <RowDefinition Height="42"/>
                                <RowDefinition Height="42"/>
                                <RowDefinition Height="42"/>
                                <RowDefinition Height="42"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Capture Area (Image)" VerticalAlignment="Center" FontSize="14" Foreground="#E0E0E0"/>
                            <TextBox x:Name="TxtShortcutArea" Grid.Row="0" Grid.Column="1" Style="{StaticResource ModernTextBox}" Height="32" IsReadOnly="True" PreviewKeyDown="Shortcut_KeyDown"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Capture Fullscreen" VerticalAlignment="Center" FontSize="14" Foreground="#E0E0E0"/>
                            <TextBox x:Name="TxtShortcutFull" Grid.Row="1" Grid.Column="1" Style="{StaticResource ModernTextBox}" Height="32" IsReadOnly="True" PreviewKeyDown="Shortcut_KeyDown"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Record Screen (Video)" VerticalAlignment="Center" FontSize="14" Foreground="#E0E0E0"/>
                            <TextBox x:Name="TxtShortcutVideo" Grid.Row="2" Grid.Column="1" Style="{StaticResource ModernTextBox}" Height="32" IsReadOnly="True" PreviewKeyDown="Shortcut_KeyDown"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Capture Text (OCR)" VerticalAlignment="Center" FontSize="14" Foreground="#E0E0E0"/>
                            <TextBox x:Name="TxtShortcutOcr" Grid.Row="3" Grid.Column="1" Style="{StaticResource ModernTextBox}" Height="32" IsReadOnly="True" PreviewKeyDown="Shortcut_KeyDown"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Toggle Desktop Icons" VerticalAlignment="Center" FontSize="14" Foreground="#E0E0E0"/>
                            <TextBox x:Name="TxtShortcutIcons" Grid.Row="4" Grid.Column="1" Style="{StaticResource ModernTextBox}" Height="32" IsReadOnly="True" PreviewKeyDown="Shortcut_KeyDown"/>
                        </Grid>
                    </Border>
                    
                    <Button Content="Reset Shortcuts to Defaults" Width="190" Height="34" HorizontalAlignment="Right" Background="#2E2E2E" Foreground="White" BorderThickness="0" Cursor="Hand" Click="ResetShortcuts_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <!-- QUICK ACCESS PANEL -->
                <StackPanel x:Name="PanelQuickAccess" Visibility="Collapsed">
                    <TextBlock Text="Quick Access Overlay" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,18"/>

                    <Border Style="{StaticResource SettingsCard}">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Thumbnail Card Size" FontSize="15" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Left"/>
                                <TextBlock x:Name="LblScaleValue" Text="80%" FontSize="14" FontWeight="Bold" Foreground="#3A82F7" HorizontalAlignment="Right"/>
                            </Grid>
                            <TextBlock Text="Adjust the display scale of the floating screenshot stack in the bottom corner." FontSize="12" Foreground="#999999" Margin="0,0,0,12" TextWrapping="Wrap"/>
                            <Slider x:Name="SliderScale" Minimum="0.5" Maximum="1.0" TickFrequency="0.05" IsSnapToTickEnabled="True" Value="0.8" ValueChanged="SliderScale_ValueChanged"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SettingsCard}">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Card Overlap Spacing" FontSize="15" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Left"/>
                                <TextBlock x:Name="LblOverlapValue" Text="20 px" FontSize="14" FontWeight="Bold" Foreground="#3A82F7" HorizontalAlignment="Right"/>
                            </Grid>
                            <TextBlock Text="Distance between stacked cards when multiple screenshots are taken." FontSize="12" Foreground="#999999" Margin="0,0,0,12" TextWrapping="Wrap"/>
                            <Slider x:Name="SliderOverlap" Minimum="10" Maximum="40" TickFrequency="2" IsSnapToTickEnabled="True" Value="20" ValueChanged="SliderOverlap_ValueChanged"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SettingsCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="180"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                <TextBlock Text="Auto-Dismiss Timer" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,4"/>
                                <TextBlock Text="Automatically close floating thumbnail stack after inactivity." FontSize="12" Foreground="#999999" TextWrapping="Wrap"/>
                            </StackPanel>
                            <ComboBox x:Name="ComboAutoClose" Grid.Column="1" Style="{StaticResource ModernComboBox}" SelectionChanged="ComboAutoClose_SelectionChanged">
                                <ComboBoxItem Content="Never"/>
                                <ComboBoxItem Content="5 seconds"/>
                                <ComboBoxItem Content="15 seconds"/>
                                <ComboBoxItem Content="30 seconds"/>
                            </ComboBox>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- RECORDING PANEL -->
                <StackPanel x:Name="PanelRecording" Visibility="Collapsed">
                    <TextBlock Text="Screen Recording &amp; Video" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,18"/>

                    <Border Style="{StaticResource SettingsCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="220"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0" VerticalAlignment="Center">
                                <TextBlock Text="Video Framerate (FPS)" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,4"/>
                                <TextBlock Text="Higher framerates provide smoother recording but larger file sizes." FontSize="12" Foreground="#999999" TextWrapping="Wrap"/>
                            </StackPanel>
                            <ComboBox x:Name="ComboFps" Grid.Column="1" Style="{StaticResource ModernComboBox}" SelectionChanged="ComboFps_SelectionChanged">
                                <ComboBoxItem Content="30 FPS"/>
                                <ComboBoxItem Content="60 FPS"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource SettingsCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="220"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0" VerticalAlignment="Center">
                                <TextBlock Text="FFmpeg Encoding Speed" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,4"/>
                                <TextBlock Text="Ultrafast ensures zero dropped frames during real-time screen capture." FontSize="12" Foreground="#999999" TextWrapping="Wrap"/>
                            </StackPanel>
                            <ComboBox x:Name="ComboPreset" Grid.Column="1" Style="{StaticResource ModernComboBox}" SelectionChanged="ComboPreset_SelectionChanged">
                                <ComboBoxItem Content="ultrafast (Recommended)"/>
                                <ComboBoxItem Content="superfast"/>
                                <ComboBoxItem Content="veryfast"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource SettingsCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="220"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,16,0" VerticalAlignment="Center">
                                <TextBlock Text="GIF Quantization Quality" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,4"/>
                                <TextBlock Text="Custom 2-pass bayer dithering generates crisp GIFs with tiny file sizes." FontSize="12" Foreground="#999999" TextWrapping="Wrap"/>
                            </StackPanel>
                            <ComboBox x:Name="ComboGifQuality" Grid.Column="1" Style="{StaticResource ModernComboBox}" SelectionChanged="ComboGifQuality_SelectionChanged">
                                <ComboBoxItem Content="High (2-Pass Bayer Dither)"/>
                                <ComboBoxItem Content="Standard"/>
                            </ComboBox>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- ABOUT PANEL -->
                <StackPanel x:Name="PanelAbout" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <Border Width="80" Height="80" CornerRadius="18" ClipToBounds="True" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <Image Source="Assets/icon_256.png" Width="80" Height="80"/>
                    </Border>
                    <TextBlock Text="GlassShot" FontSize="26" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                    <TextBlock Text="Version 1.0.0 (CleanShot X Parity Engine)" FontSize="14" Foreground="#3A82F7" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,25"/>
                    <TextBlock Text="The ultimate agentic screen capture, annotation, &amp; video recording suite for Windows." FontSize="14" Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Built with C#, WPF (.NET 8), SkiaSharp &amp; FFmpeg by Antigravity." FontSize="13" Foreground="#888888" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
